{
  "tasks": {
    "build": "mkdir -p dist && deno bundle ./src/main.ts -o dist/app.js && cp src/index.html dist/ && cp -r src/assets dist/",
    "build:check": "deno check ./src/main.ts",
    "build:docker": "cd ../../ && docker build -f sample/basic/Dockerfile -t parallax-bg-demo .",
    "run:docker": "deno task build:docker && docker run -p 8080:80 parallax-bg-demo",
    "clean": "rm -rf dist",
    "serve:dist": "cd dist && python3 -m http.server 8000",
    "fmt": "deno fmt",
    "lint": "deno lint"
  },
  "imports": {
    "@std/http": "jsr:@std/http@1"
  },
  "compilerOptions": {
    "strict": true,
    "lib": ["dom", "dom.iterable", "es2022", "deno.ns"]
  }
}
